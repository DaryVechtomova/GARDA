import React from 'react'
import { TbArrowRight, TbArrowUpSquare } from 'react-icons/tb' // Note: TbArrowUpSquare is imported but not used in the returned JSX

const ProductHd = ({product}) => { // Destructuring props directly
    return (
        <div className='max-padd-container flex items-center flex-wrap gap-x-2 medium-16 py-5 capitalize bg-primary rounded-tl-xl rounded-tr-xl'>
            
        </div>
    )
}

export default ProductHd